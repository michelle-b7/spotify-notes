import {useEffect, useState} from 'react';
import {Save, FileText, Check} from 'lucide-react';
import {getNote, saveNote} from '../storage/notesStorage';

// use the lucide react components to help make this note editing component

export default function NoteEditor({trackId}){
    return(
        <p></p>
    );
}